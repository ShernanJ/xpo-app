# Onboarding Lib Notes

`apps/web/lib/onboarding/chatAgent.ts` was retired.

Current chat orchestration lives under `apps/web/lib/agent-v2/`, with the main coordinator at `apps/web/lib/agent-v2/orchestrator/conversationManager.ts`.

If you see references to `chatAgent.ts` in old planning docs, treat them as historical notes.
